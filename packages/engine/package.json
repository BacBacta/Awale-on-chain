{
  "name": "@awale/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic Awalé (Oware Abapa) rule engine — TypeScript port, byte-identical to the on-chain Solidity engine.",
  "main": "src/awale.ts",
  "types": "src/awale.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "viem": "^2.21.0",
    "vitest": "^2.1.0"
  }
}
