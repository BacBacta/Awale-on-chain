{
  "name": "@awale/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared Awalé protocol primitives: EIP-712 digests, token config, contract ABIs, types — single source of truth for the server and the mini-app.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
